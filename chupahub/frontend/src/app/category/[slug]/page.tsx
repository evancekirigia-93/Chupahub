import {ProductCard} from '@/components/Site';import {categories,products} from '@/lib/data';
export function generateStaticParams(){return categories.map(c=>({slug:c.slug}))}
export default function Category({params}:{params:{slug:string}}){const list=products.filter(p=>p.category===params.slug||params.slug==='whisky');return <main className="max-w-7xl mx-auto px-4 py-10"><h1 className="text-5xl font-black capitalize">{params.slug.replace('-',' ')}</h1><div className="glass rounded-2xl p-4 my-6 grid md:grid-cols-5 gap-3"><input className="bg-white/10 rounded-xl p-3" placeholder="Filter search"/><select className="bg-black rounded-xl p-3"><option>Sort</option></select><select className="bg-black rounded-xl p-3"><option>Brand</option></select><select className="bg-black rounded-xl p-3"><option>Country</option></select><select className="bg-black rounded-xl p-3"><option>ABV</option></select></div><div className="grid md:grid-cols-4 gap-5">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div></main>}
