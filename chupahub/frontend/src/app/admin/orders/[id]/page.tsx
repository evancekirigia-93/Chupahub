'use client';
import Link from 'next/link'; import { useCallback,useEffect,useMemo,useState } from 'react'; import { createBrowserSupabase } from '@/lib/supabase-browser'; import { money } from '@/lib/supabase';
export default function OrderDetail({params}:{params:Promise<{id:string}>}){const supabase=useMemo(()=>createBrowserSupabase(),[]);const [id,setId]=useState(''),[order,setOrder]=useState<any>(null),[note,setNote]=useState(''),[rider,setRider]=useState(''),[phone,setPhone]=useState(''),[error,setError]=useState('');useEffect(()=>{void params.then(x=>setId(x.id))},[params]);const load=useCallback(async()=>{if(!supabase||!id)return;const {data,error}=await supabase.from('orders').select('*,order_items(*),payments(*),order_status_history(*),order_notifications(*)').eq('id',id).single();if(error)setError(error.message);else setOrder(data)},[supabase,id]);useEffect(()=>{void load()},[load]);async function action(status:string){if(status==='cancelled'&&!confirm('Cancel this order?'))return;const {data:{session}}=await supabase!.auth.getSession();const response=await fetch(`/api/admin/orders/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`},body:JSON.stringify({status,note,riderName:rider,riderPhone:phone})});const result=await response.json();if(!response.ok)setError(result.error);else void load()}if(!order)return <main className="p-8">{error||'Loading order…'}</main>;const next:any={pending:['confirmed','cancelled'],pending_payment:['confirmed','cancelled'],paid:['confirmed','cancelled'],confirmed:['processing','cancelled'],processing:['dispatched','cancelled'],dispatched:['delivered']};return <main className="mx-auto max-w-5xl p-5"><Link href="/admin/orders" className="font-bold text-brand-orange">← Orders</Link><div className="mt-4 rounded-2xl bg-white p-6 shadow-card"><h1 className="text-3xl font-black">{order.order_number||order.id}</h1><p>{new Date(order.created_at).toLocaleString()} · {order.status}</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><h2 className="font-black">Customer</h2><p>{order.customer_name||'Guest'}</p><a href={`tel:${order.customer_phone}`} className="text-brand-orange">{order.customer_phone}</a><p>{order.customer_email}</p></div><div><h2 className="font-black">Delivery</h2><p>{order.delivery_address}</p>{order.gps_lat&&<a target="_blank" rel="noreferrer" className="text-brand-orange" href={`https://www.google.com/maps/search/?api=1&query=${order.gps_lat},${order.gps_lng}`}>Open in Google Maps</a>}</div></div><div className="mt-5 border-t pt-4">{order.order_items.map((item:any)=><p key={item.id} className="flex justify-between py-1"><span>{item.quantity} × {item.product_name}</span><b>{money(item.line_total)}</b></p>)}<p className="mt-3 flex justify-between">Subtotal <b>{money(order.subtotal)}</b></p><p className="flex justify-between">Discount <b>{money(order.discount_total)}</b></p><p className="flex justify-between">Delivery <b>{money(order.delivery_fee)}</b></p><p className="flex justify-between text-xl font-black">Total <b>{money(order.total)}</b></p></div><p className="mt-4">Payment: <b>{order.payment_method} · {order.payment_status}</b></p>{order.gift_note&&<p className="mt-2">Gift note: {order.gift_note}</p>}<textarea value={note} onChange={e=>setNote(e.target.value)} className="mt-5 w-full rounded-xl border p-3" placeholder="Optional status note"/>{order.status==='processing'&&<div className="mt-3 grid gap-3 sm:grid-cols-2"><input value={rider} onChange={e=>setRider(e.target.value)} className="rounded-xl border p-3" placeholder="Rider name"/><input value={phone} onChange={e=>setPhone(e.target.value)} className="rounded-xl border p-3" placeholder="Rider phone"/></div>}<div className="mt-5 flex flex-wrap gap-3">{(next[order.status]||[]).map((status:string)=><button key={status} onClick={()=>void action(status)} className="rounded-xl bg-brand-orange px-4 py-3 font-black text-white">Mark as {status}</button>)}</div>{error&&<p className="mt-3 text-red-600">{error}</p>}</div><section className="mt-5 rounded-2xl bg-white p-6 shadow-card"><h2 className="text-xl font-black">Status & notification history</h2>{order.order_status_history?.map((entry:any)=><p key={entry.id} className="border-b py-2">{entry.from_status||'New'} → {entry.to_status} · {new Date(entry.created_at).toLocaleString()}</p>)}{order.order_notifications?.map((entry:any)=><p key={entry.id} className="border-b py-2">{entry.channel}: {entry.status} {entry.error_message&&`— ${entry.error_message}`}</p>)}</section></main>}
